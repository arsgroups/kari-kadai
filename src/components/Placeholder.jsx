export default function Placeholder({ title }) {
  return (
    <div className="page">
      <h1>{title}</h1>
      <p className="muted">This module hasn't been built yet — coming up next.</p>
    </div>
  )
}
